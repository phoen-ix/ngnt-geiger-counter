FROM python:3.13.12-slim

COPY scripts/pm2/requirements.txt /opt/requirements.txt
RUN pip install --no-cache-dir -r /opt/requirements.txt && \
    useradd -r -s /usr/sbin/nologin subscriber

USER subscriber

CMD ["python3", "/opt/scripts/mqtt_bro_impulses.py"]
